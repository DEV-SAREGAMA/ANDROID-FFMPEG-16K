#include <stdint.h>

/**
 * Stubbed resources for filtergraph HTML/CSS.
 * Upstream these are generated from fftools/resources/graph.css(.res)
 * and graph.html(.res) at build time; for our Android wrapper we don't
 * actually need the real content, just valid symbols so resman.c links.
 */

// HTML resource
const unsigned char ff_graph_html_data[] = "";
const unsigned int ff_graph_html_len = 0;

// CSS resource
const unsigned char ff_graph_css_data[] = "";
const unsigned ff_graph_css_len = 0;
